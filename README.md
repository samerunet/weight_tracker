
# NEON FIT — Web (Next.js + Tailwind)

A single-repo, pure web implementation of the Neon Fit dashboard with 3D neon look:
- Home: stat cards, gradient ring, actions, tiles
- Trends: weight chart + add weight (localStorage)
- Log: Open Food Facts barcode lookup (manual code input)
- Workouts: sample plan
- Coach: daily calories calculator

## Quick start
```bash
npm install
npm run dev
# open http://localhost:3000
```
